version: "3.9"
services:
db:
  image: mysql:8.4
  environment:
    MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
volumes:
  - db_data:/var/lib/mysql
  - ./db/init:/docker-entrypoint-initdb.d:ro
healthcheck:
  test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1"]
  interval: 5s
  timeout: 3s
  retries: 20

backend:
  image: chalijay/lempbackend:latest
  environment:
    DB_HOST: db
    DB_USER: appuser
    DB_PASSWORD: ${DB_PASSWORD}
    DB_NAME: appdb
  depends_on:
    db:
    condition: service_healthy
frontend:
  image: chalijay/lempfrontend:latest
  depends_on:
    - backend
  ports:
    - "80:80" # expose only frontend to the host
  volumes:
    db_data: